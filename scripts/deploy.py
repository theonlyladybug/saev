"""
Script to move various webapp-related stuff into the docs folder where it' supposed to go.
"""


def main():
    """
    Assumes the build-classification step has run (justfile).
    """
    # Copy everything from web/apps/classification into docs/webapps/classification. AI!
    breakpoint()


if __name__ == "__main__":
    main()
